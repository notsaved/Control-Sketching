# Control-Sketches
Interactive visualization of 450 outputs of the Samsung Sketch-to-Image app. The filterable grid-based visualization allows to compare outputs across control sketches and diffusion styles.
